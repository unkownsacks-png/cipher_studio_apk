package com.cipher.studio.ui.theme

import androidx.compose.ui.graphics.Color

// --- CIPHER ELITE PALETTE ---

// Primary Brand Colors (Electric & Professional)
val CipherBlue = Color(0xFF2563EB)      // Primary Action (Send, Active)
val CipherBlueLight = Color(0xFF60A5FA) // Accents / Highlights
val CipherPurple = Color(0xFF7C3AED)    // AI / Magic / Gradient Start
val CipherPink = Color(0xFFDB2777)      // Gradient End

// Security & Status Colors
val CipherGreen = Color(0xFF059669)     // Success / Secure / Elite Status
val CipherRed = Color(0xFFDC2626)       // Error / Threat / Stop
val CipherOrange = Color(0xFFD97706)    // Warning / Processing

// --- DARK MODE SURFACES (Deep Space Theme) ---
// Not just black, but "Rich Black" for depth
val DarkBackground = Color(0xFF02040A)  // The absolute background (Deepest)
val DarkSurface = Color(0xFF0B0F19)     // Cards / Sidebar / Input Area
val DarkSurfaceHighlight = Color(0xFF161E2E) // Hover states / Selected items
val DarkTextPrimary = Color(0xFFF9FAFB) // Main Text (Off-white for eye comfort)
val DarkTextSecondary = Color(0xFF9CA3AF) // Subtitles / Hints

// --- LIGHT MODE SURFACES (Clean Lab Theme) ---
val LightBackground = Color(0xFFF8FAFC) // Cool Gray / Ice
val LightSurface = Color(0xFFFFFFFF)    // Pure White Cards
val LightSurfaceHighlight = Color(0xFFF1F5F9) 
val LightTextPrimary = Color(0xFF111827) // Deep Charcoal (Better than black)
val LightTextSecondary = Color(0xFF6B7280)

// --- CHAT BUBBLES ---
// User Bubble (Modern Blue Gradient Feel)
val UserBubbleDark = Color(0xFF2563EB)
val UserBubbleLight = Color(0xFF2563EB)

// AI Bubble (Subtle, let the text shine)
val AIBubbleDark = Color(0xFF1F2937)
val AIBubbleLight = Color(0xFFFFFFFF)

// --- GLASSMORPHISM & OVERLAYS ---
val GlassBlack = Color(0xCC02040A) // 80% Opacity Dark
val GlassWhite = Color(0xCCFFFFFF) // 80% Opacity Light
val BorderDark = Color(0xFF1F2937)
val BorderLight = Color(0xFFE5E7EB)